# Port for the local WebSocket server (default: 8788)
ANCHOR_PORT=8788

# Orbit relay URL. Leave empty to run anchor in local-only mode.
ANCHOR_ORBIT_URL=

# Shared bearer token for anchor -> orbit auth in local mode
ZANE_ANCHOR_JWT_SECRET=

# Working directory for codex app-server (default: current directory)
ANCHOR_APP_CWD=
